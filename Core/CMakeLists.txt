# Core directory

# Contains geometry base class and services, taken from artg4

# Create a library called "gm2geom_core"
art_make(LIBRARIES)

# Install header files into the products area
install_headers() 
